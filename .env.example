# Connection string do Neon (painel do projeto > Connect)
DATABASE_URL=postgresql://USUARIO:SENHA@ep-xxxxx.regiao.aws.neon.tech/neondb?sslmode=require

# Porta do servidor
PORT=3000

# Segredo dos tokens JWT (troque por um valor forte em produção)
JWT_SECRET=troque-por-um-segredo-forte
